<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/ActivityAnimationStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ActivityAnimationStyle" parent="@android:style/Animation.Activity">
        <!--activity还没有finish()下退出的效果-->
        <item name="android:activityOpenExitAnimation">@anim/activity_outt</item>
        <!-- 新的activity创建进入效果 -->
        <item name="android:activityOpenEnterAnimation">@anim/activity_inn</item>
        <!-- 上一个 activity返回进入效果-->
        <item name="android:activityCloseEnterAnimation">@anim/activity_inn</item>
        <!--activity finish()之后退出效果-->
        <item name="android:activityCloseExitAnimation">@anim/activity_outt</item>

    </style>

</resources>
